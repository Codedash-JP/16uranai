---
const { title, url, tags = [] } = Astro.props;
const shareUrl = `https://x.com/intent/tweet?text=${encodeURIComponent(title)}&url=${encodeURIComponent(url)}${tags.length ? `&hashtags=${encodeURIComponent(tags.join(','))}` : ''}`;
---
<a href={shareUrl} target="_blank" rel="noopener noreferrer" class="btn">Share on X</a>
