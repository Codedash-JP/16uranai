import { defineConfig } from 'astro/config';

// Update "site" to your actual Pages URL after first deploy if needed.
export default defineConfig({
  site: 'https://Codedash-JP.github.io/16p-astro',
  base: '/16p-astro',
});
